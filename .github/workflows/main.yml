name: First workflow 

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcome message continue-on-error: 
        run: echo "welcome to our first github actions workflow" 
        
    
